# Frontend Development Dockerfile with Hot Reload
# Uses Vite dev server for HMR (Hot Module Replacement)

FROM node:20-alpine

WORKDIR /app/frontend

# Copy package files (host context is root)
COPY frontend/package*.json ./

# Copy shared package
COPY shared ../shared

# Install all dependencies
RUN npm install

# Copy source files (will be overwritten by volume mount in dev)
COPY frontend .

EXPOSE 5173

# Use Vite dev server with host binding for Docker networking
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
